# Reference Materials

This folder keeps non-runtime reference assets out of the repository root.

- `UAID_Mercamio_PROYECTO_COMPLETO_FINAL.pdf`: original project reference document.
- `skills/`: saved HTML reference pages for agent skills that were previously stored in the root.

Runtime code, scripts, and database assets stay in `src/`, `scripts/`, and `db/`.
