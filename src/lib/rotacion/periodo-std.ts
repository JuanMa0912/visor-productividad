export type RotacionPeriodoStdMeta = {
  periodoStart: string;
  periodoEnd: string;
  refreshedAt: string;
  rowCount: number;
};

export type RotacionDataSource = "periodo-std" | "matview" | "raw";
